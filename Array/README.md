# Java Array Element Search – Example Program

This repository contains a simple Java program that demonstrates how to **search for an element in an array** and determine its position.  
It is intended for beginners who are learning arrays and basic control flow in Java.

---

## 📌 Program Overview

The program works with an integer array and searches for a specific value within it.  
Once the element is found, its **position in the array** is determined and displayed.

The program also prints all array elements along with their corresponding positions.

---

## 🧪 Code Functionality

- Declares and initializes an integer array.
- Uses a `for` loop to search for a specific element in the array.
- Stops searching once the element is found using the `break` statement.
- Prints each array element along with its position.
- Calculates and displays the position of the searched element.

---

## 🖥️ Output

The program output is shown below:

![Program Output](output.png)

---

## 📂 File Information

- `Find.java` — Java source code (package: `Array`)
- `output.png` — Screenshot of the program output
- `README.md` — Project documentation

---

## 👨‍💻 Author

**Tejas Halvankar**  
📧 Email: `tejashalvankar0@gmail.com`  
🌐 GitHub: [Tejas-H01](https://github.com/Tejas-H01)
